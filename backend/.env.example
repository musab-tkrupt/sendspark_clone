SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=Videos
# Optional prefix for non-preview uploads (e.g. flat MP4 fallbacks). Lead preview pack is always at previews/{slug}/{id}/…
SUPABASE_PATH_PREFIX=sendspark
# Canonical host for /v/{lead}/{previewId} links in metadata (default tkrupt.com)
FRONTEND_URL=https://tkrupt.com