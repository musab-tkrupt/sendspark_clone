# Optional: server-only API base for Next.js /api/config (recommended on Vercel — read at runtime, no rebuild when URL changes).
BACKEND_URL=http://localhost:8000

# Browser bundle fallback (inlined at `next build`; changing on Vercel requires redeploy).
NEXT_PUBLIC_API_URL=http://localhost:8000
